import { PageHeader } from '../../components/ui/PageHeader';

export const DashboardPage = () => (
  <PageHeader
    title="Dashboard"
    description="This page will contain KPI cards, statistics, and chart placeholders later."
  />
);
