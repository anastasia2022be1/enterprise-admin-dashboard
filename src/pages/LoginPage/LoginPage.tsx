import { PageHeader } from '../../components/ui/PageHeader';

export const LoginPage = () => (
  <PageHeader
    title="Login"
    description="Authentication flow will be implemented in the next stage."
  />
);
